# AlgoHub 接口文档

## 目录
- [通用说明](#通用说明)
- [鉴权模块](#鉴权模块)
- [用户模块](#用户模块)
- [题目模块](#题目模块)
- [题解模块](#题解模块)
- [管理模块](#管理模块)

---

## 通用说明

### 基础信息
- **基础URL**: `http://localhost:8080`
- **数据格式**: JSON
- **字符编码**: UTF-8

### 响应格式
所有接口统一返回以下格式：

```json
{
  "code": 1,      
  "message": "success",  
  "data": {}     
}
```

### 分页参数说明
分页查询接口支持以下参数：

| 参数 | 类型 | 默认值 | 描述 |
|---|---|---|---|
| current | Integer | 1 | 当前页码 |
| size | Integer | 10 | 每页大小 |
| keyword | String | null | 搜索关键词 |
| sortField | String | null | 排序字段 |
| sortOrder | String | null | 排序方式：asc/desc |

### 分页响应格式
```json
{
  "current": 1,    
  "size": 10,     
  "total": 100,      
  "pages": 10,       
  "records": []      
}
```

---

## 鉴权模块

### 1. 用户登录

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/auth/login` |
| 请求方式 | `POST` |
| 接口描述 | 用户登录验证 |

**请求参数**

请求体为User对象，包含以下字段：

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |

**请求示例**
```bash
POST /auth/login
Content-Type: application/json

{
  "username": "testuser",
  "password": "123456"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "登录失败",
  "data": null
}
```

### 2. 用户注册

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/auth/register` |
| 请求方式 | `POST` |
| 接口描述 | 新用户注册 |

**请求参数**

请求体为User对象，包含以下字段：

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| username | String | 是 | 用户名 |
| password | String | 是 | 密码 |
| email | String | 否 | 邮箱地址 |
| role | Integer | 否 | 用户角色（默认为0） |

**请求示例**
```bash
POST /auth/register
Content-Type: application/json

{
  "username": "newuser",
  "password": "123456",
  "email": "newuser@example.com"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "注册失败",
  "data": null
}
```

---

## 用户模块

### 1. 获取用户列表

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/users/list` |
| 请求方式 | `GET` |
| 接口描述 | 获取所有用户列表 |

**请求参数**
无

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": [
    {
      "id": 1,
      "uid": 10001,
      "username": "admin",
      "email": "admin@example.com",
      "role": 1,
      "points": 100,
      "status": 1
    }
  ]
}
```

### 2. 分页查询用户

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/users/page` |
| 请求方式 | `GET` |
| 接口描述 | 分页查询用户列表 |

**请求参数**

| 参数 | 类型 | 必填 | 默认值 | 描述 |
|---|---|---|---|---|
| page | Integer | 否 | 1 | 页码 |
| size | Integer | 否 | 10 | 每页大小 |

**请求示例**
```bash
GET /api/users/page?page=1&size=20
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 20,
    "total": 50,
    "pages": 3,
    "records": [
      {
        "id": 1,
        "uid": 10001,
        "username": "admin",
        "email": "admin@example.com",
        "role": 1,
        "points": 100,
        "status": 1
      }
    ]
  }
}
```

### 3. 搜索用户

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/users/search` |
| 请求方式 | `POST` |
| 接口描述 | 根据条件搜索用户 |

**请求参数**

请求体为 PageQuery 对象，参见[分页参数说明](#分页参数说明)

**请求示例**
```json
{
  "current": 1,
  "size": 10,
  "keyword": "admin",
  "sortField": "username",
  "sortOrder": "asc"
}
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 10,
    "total": 5,
    "pages": 1,
    "records": [
      {
        "id": 1,
        "uid": 10001,
        "username": "admin",
        "email": "admin@example.com",
        "role": 1,
        "points": 100,
        "status": 1
      }
    ]
  }
}
```

### 4. 更新用户

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/users` |
| 请求方式 | `PUT` |
| 接口描述 | 更新用户信息 |
| 状态 | 待实现 |

**请求参数**

请求体为User对象，包含需要更新的用户信息

**请求示例**
```bash
PUT /api/users
Content-Type: application/json

{
  "id": 1,
  "username": "updateduser",
  "email": "updated@example.com"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "更新失败",
  "data": null
}
```

### 5. 收藏题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/users/star` |
| 请求方式 | `GET` |
| 接口描述 | 收藏题解 |
| 状态 | 待实现 |

---

## 题目模块

### 1. 获取题目列表

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems/list` |
| 请求方式 | `GET` |
| 接口描述 | 获取所有题目列表 |

**请求参数**
无

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": [
    {
      "id": 1,
      "pid": 100001,
      "title": "两数之和",
      "description": "给定一个整数数组...",
      "difficulty": 1,
      "tags": "数组,哈希表",
      "platform": "LeetCode",
      "problemSet": "L1",
      "originUrl": "https://leetcode.com/problems/two-sum"
    }
  ]
}
```

### 2. 分页查询题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems/page` |
| 请求方式 | `GET` |
| 接口描述 | 分页查询题目列表 |

**请求参数**

| 参数 | 类型 | 必填 | 默认值 | 描述 |
|---|---|---|---|---|
| page | Integer | 否 | 1 | 页码 |
| size | Integer | 否 | 10 | 每页大小 |

**请求示例**
```bash
GET /api/problems/page?page=1&size=20
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 20,
    "total": 100,
    "pages": 5,
    "records": [
      {
        "id": 1,
        "pid": 100001,
        "title": "两数之和",
        "description": "给定一个整数数组...",
        "difficulty": 1,
        "tags": "数组,哈希表",
        "platform": "LeetCode",
        "problemSet": "L1",
        "originUrl": "https://leetcode.com/problems/two-sum"
      }
    ]
  }
}
```

### 3. 搜索题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems/search` |
| 请求方式 | `POST` |
| 接口描述 | 根据条件搜索题目 |

**请求参数**

请求体为 PageQuery 对象，参见[分页参数说明](#分页参数说明)

**请求示例**
```bash
POST /api/problems/search

{
  "current": 1,
  "size": 10,
  "keyword": "数组",
  "sortField": "difficulty",
  "sortOrder": "asc"
}
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 10,
    "total": 30,
    "pages": 3,
    "records": [
      {
        "id": 1,
        "pid": 100001,
        "title": "两数之和",
        "description": "给定一个整数数组...",
        "difficulty": 1,
        "tags": "数组,哈希表",
        "platform": "LeetCode",
        "problemSet": "L1",
        "originUrl": "https://leetcode.com/problems/two-sum"
      }
    ]
  }
}
```

### 4. 根据ID获取题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems/{problem_id}` |
| 请求方式 | `GET` |
| 接口描述 | 根据题目ID获取题目详情 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| problem_id | Integer | 是 | 题目ID（路径参数） |

**请求示例**
```bash
GET /api/problems/100001
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "id": 1,
    "pid": 100001,
    "title": "两数之和",
    "description": "给定一个整数数组...",
    "difficulty": 1,
    "tags": "数组,哈希表",
    "platform": "LeetCode",
    "problemSet": "L1",
    "originUrl": "https://leetcode.com/problems/two-sum"
  }
}
```

### 5. 添加题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems` |
| 请求方式 | `POST` |
| 接口描述 | 添加新题目 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| pid | Long | 是 | 题目编号（唯一） |
| title | String | 是 | 题目标题 |
| description | String | 是 | 题目描述 |
| difficulty | Integer | 否 | 难度等级（1-5） |
| tags | String | 否 | 标签，逗号分隔 |
| platform | String | 否 | 平台来源 |
| problemSet | String | 否 | 题目集 |
| originUrl | String | 否 | 原题链接 |

**请求示例**
```json
{
  "pid": 100002,
  "title": "测试题目",
  "description": "题目描述",
  "difficulty": 3,
  "tags": "算法,排序",
  "platform": "LeetCode",
  "problemSet": "L1",
  "originUrl": "https://leetcode.com/problems/test-problem"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "",
  "data": null
}
```

### 6. 更新题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems` |
| 请求方式 | `PATCH` |
| 接口描述 | 更新题目信息 |

**请求参数**

同添加题目接口，但需要包含题目ID

**请求示例**
```json
{
  "id": 1,
  "pid": 100001,
  "title": "两数之和（更新）",
  "description": "更新后的题目描述",
  "difficulty": 2,
  "tags": "数组,哈希表,双指针",
  "platform": "LeetCode",
  "problemSet": "L1",
  "originUrl": "https://leetcode.com/problems/two-sum"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "更新失败",
  "data": null
}
```

### 7. 删除题目

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/problems/{id}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除指定题目 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| id | Long | 是 | 题目ID（路径参数） |

**请求示例**
```bash
DELETE /api/problems/1
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "删除失败",
  "data": null
}
```

---

## 题解模块

### 1. 获取题解列表

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/list` |
| 请求方式 | `GET` |
| 接口描述 | 获取所有题解列表 |

### 1.1. 根据题目ID获取题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/{pid}` |
| 请求方式 | `GET` |
| 接口描述 | 根据题目ID获取题解列表 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| pid | Long | 是 | 题目ID（路径参数） |

**请求示例**
```bash
GET /api/solutions/100001
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": [
    {
      "id": 1,
      "pid": 100001,
      "submitterId": 10001,
      "problemName": "两数之和",
      "contentUrl": "/solutions/1.md",
      "submitId": 1001,
      "submitTime": "2024-01-01 12:00:00",
      "updateTime": "2024-01-01 12:00:00"
    }
  ]
}
```

**请求参数**
无

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": [
    {
      "id": 1,
      "pid": 100001,
      "submitterId": 10001,
      "problemName": "两数之和",
      "contentUrl": "/solutions/1.md",
      "submitId": 1001,
      "submitTime": "2024-01-01 12:00:00",
      "updateTime": "2024-01-01 12:00:00"
    }
  ]
}
```

### 2. 分页查询题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/page` |
| 请求方式 | `GET` |
| 接口描述 | 分页查询题解列表 |

**请求参数**

| 参数 | 类型 | 必填 | 默认值 | 描述 |
|---|---|---|---|---|
| page | Integer | 否 | 1 | 页码 |
| size | Integer | 否 | 10 | 每页大小 |

**请求示例**
```bash
GET /api/solutions/page?page=1&size=20
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 20,
    "total": 50,
    "pages": 3,
    "records": [
      {
        "id": 1,
        "pid": 100001,
        "submitterId": 10001,
        "problemName": "两数之和",
        "contentUrl": "/solutions/1.md",
        "submitId": 1001,
        "submitTime": "2024-01-01 12:00:00",
        "updateTime": "2024-01-01 12:00:00"
      }
    ]
  }
}
```

### 3. 搜索题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/search` |
| 请求方式 | `POST` |
| 接口描述 | 根据条件搜索题解 |

**请求参数**

请求体为 PageQuery 对象，参见[分页参数说明](#分页参数说明)

**请求示例**
```json
{
  "current": 1,
  "size": 10,
  "keyword": "动态规划",
  "sortField": "submitTime",
  "sortOrder": "desc"
}
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "current": 1,
    "size": 10,
    "total": 15,
    "pages": 2,
    "records": [
      {
        "id": 1,
        "pid": 100001,
        "submitterId": 10001,
        "problemName": "两数之和",
        "contentUrl": "/solutions/1.md",
        "submitId": 1001,
        "submitTime": "2024-01-01 12:00:00",
        "updateTime": "2024-01-01 12:00:00"
      }
    ]
  }
}
```

### 4. 提交题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions` |
| 请求方式 | `POST` |
| 接口描述 | 提交新题解（文本方式） |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| pid | Integer | 是 | 题目ID |
| submitterId | Integer | 是 | 提交者ID |
| problemName | String | 是 | 题目名称 |
| contentUrl | String | 是 | 题解内容URL |
| submitId | Integer | 否 | 提交ID |
| submitTime | Date | 否 | 提交时间 |
| updateTime | Date | 否 | 更新时间 |

**请求示例**
```json
{
  "pid": 100001,
  "submitterId": 10001,
  "problemName": "两数之和",
  "contentUrl": "/solutions/new.md",
  "submitId": 1002
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "添加失败",
  "data": null
}
```

### 4.1. 通过文件提交题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/upload` |
| 请求方式 | `POST` |
| 接口描述 | 通过文件提交新题解 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| file | File | 是 | 题解文件 |

**请求示例**
```
POST /api/solutions/upload
Content-Type: multipart/form-data

file: [题解文件]
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "添加失败",
  "data": null
}
```

### 5. 更新题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions` |
| 请求方式 | `PUT` |
| 接口描述 | 更新题解信息 |

**请求参数**

同提交题解接口，但需要包含题解ID

**请求示例**
```json
{
  "id": 1,
  "pid": 100001,
  "submitterId": 10001,
  "problemName": "两数之和",
  "contentUrl": "/solutions/updated.md",
  "submitId": 1002,
  "updateTime": "2024-01-02 12:00:00"
}
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "更新失败",
  "data": null
}
```

### 6. 删除题解

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/{id}` |
| 请求方式 | `DELETE` |
| 接口描述 | 删除指定题解 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| id | Long | 是 | 题解ID（路径参数） |

**请求示例**
```
DELETE /api/solutions/1
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": null
}
```

失败响应：
```json
{
  "code": 0,
  "message": "删除失败",
  "data": null
}
```

### 7. 获取题解详情 🆕

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/api/solutions/detail/{id}` |
| 请求方式 | `GET` |
| 接口描述 | 获取题解详情信息，包含contentUrl用于文件访问 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| id | Long | 是 | 题解ID（路径参数） |

**请求示例**
```
GET /api/solutions/detail/1
```

**响应示例**

成功响应：
```json
{
  "code": 1,
  "message": "success",
  "data": {
    "id": 1,
    "solId": 100101,
    "pid": 1001,
    "uid": 10001,
    "language": "Java",
    "contentUrl": "solutions/1001/100101.md",
    "submitId": 12345,
    "submitTime": "2025-11-01T10:00:00",
    "updateTime": "2025-11-01T10:00:00"
  }
}
```

**说明**
- contentUrl 可以通过静态资源映射直接访问，例如：`http://localhost:8080/solutions/1001/100101.md`
- 支持在线预览和下载

---

## 管理模块

### 1. 封禁用户

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/manage/ban/{uid}` |
| 请求方式 | `GET` |
| 接口描述 | 封禁指定用户 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| uid | Long | 是 | 用户ID（路径参数） |

**请求示例**
```
GET /manage/ban/10001
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": "用户10001已被封禁"
}
```

### 2. 解封用户 🆕

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/manage/unban/{uid}` |
| 请求方式 | `GET` |
| 接口描述 | 解封指定用户 |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| uid | Long | 是 | 用户ID（路径参数） |

**请求示例**
```
GET /manage/unban/10001
```

**响应示例**
```json
{
  "code": 1,
  "message": "success",
  "data": "用户10001已被解封"
}
```

### 3. 审核题解 🆕

**基本信息**

| 字段 | 描述 |
|---|---|
| 接口路径 | `/manage/audit/{submitId}` |
| 请求方式 | `GET` |
| 接口描述 | 审核题解提交（通过或拒绝） |

**请求参数**

| 参数 | 类型 | 必填 | 描述 |
|---|---|---|---|
| submitId | Long | 是 | 提交ID（路径参数） |
| result | Boolean | 是 | 审核结果：true-通过，false-拒绝（查询参数） |
| reason | String | 否 | 拒绝原因（仅result=false时需要，查询参数） |

**请求示例**

审核通过：
```
GET /manage/audit/12345?result=true
```

审核拒绝：
```
GET /manage/audit/12345?result=false&reason=代码质量不符合要求
```

**响应示例**

通过响应：
```json
{
  "code": 1,
  "message": "success",
  "data": "提交12345已审核通过!"
}
```

拒绝响应：
```json
{
  "code": 1,
  "message": "success",
  "data": "提交12345未通过,原因:代码质量不符合要求"
}
```

**说明**
- 审核通过后，题解文件会从 `pending-solutions/` 移动到 `solutions/{pid}/` 目录
- 题解记录会保存到 solutions 表中

## 错误码说明

| 错误码 | 描述 |
|---|---|
| 0 | 操作失败 |
| 1 | 操作成功 |

## 注意事项

1. 所有接口均需要进行身份验证（除登录、注册接口外）
2. 请求头需要包含认证信息（待实现）
3. 对于分页接口，如果不传递分页参数，将使用默认值
4. 删除操作不可恢复，请谨慎使用
5. 部分接口功能尚未完全实现，已在文档中标注

## 静态资源访问 🆕

系统配置了静态资源映射，支持直接访问存储的文件：

| URL路径 | 文件系统路径 | 说明 |
|---------|-------------|------|
| `/solutions/**` | `data/solutions/` | 已审核通过的题解文件 |
| `/pending-solutions/**` | `data/pending-solutions/` | 待审核的题解文件 |
| `/users/**` | `data/users/` | 用户相关文件 |

**访问示例**
```
# 直接访问题解文件
GET http://localhost:8080/solutions/1001/100101.md

# 访问待审核题解
GET http://localhost:8080/pending-solutions/12345.md
```

## 更新记录

| 版本 | 日期 | 更新内容 |
|---|---|---|
| v1.1.0 | 2025-11-01 | 新增题解详情接口、解封用户接口、审核题解接口；添加静态资源映射配置 |
| v1.0 | 2024-10-16 | 初始版本，完成基础接口文档编写 |